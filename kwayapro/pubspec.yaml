name: kwayapro
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Navigation
  go_router: ^13.2.0
  animations: ^2.0.1

  # Firebase (FlutterFire)
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  firebase_messaging: ^15.0.0
  google_sign_in: ^6.2.1

  # Audio
  just_audio: ^0.9.36
  audio_session: ^0.1.18
  record: ^6.0.0

  # File handling
  file_picker: ^8.0.0
  image_picker: ^1.2.2

  # Local storage
  hive_flutter: ^1.1.0
  shared_preferences: ^2.5.5
  path_provider: ^2.1.2

  # Networking
  http: ^1.2.1
  dio: ^5.4.3+1

  # System/Device
  share_plus: ^7.2.2
  flutter_local_notifications: ^17.0.0
  permission_handler: ^11.3.0
  connectivity_plus: ^7.1.1

  # Utilities
  intl: ^0.19.0
  uuid: ^4.3.3
  shimmer: ^3.0.0
  google_fonts: ^6.2.0
  rxdart: ^0.28.0
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.9
  riverpod_generator: ^2.3.9
  hive_generator: ^2.0.1
  fake_cloud_firestore: ^3.0.0
  firebase_auth_mocks: ^0.14.0
  mocktail: ^1.0.4
  path_provider_platform_interface: ^2.1.2
  connectivity_plus_platform_interface: ^2.1.0
  plugin_platform_interface: ^2.1.8
  flutter_launcher_icons: ^0.14.4

# Phase 6b: generates real Android launcher icons (replacing the stock
# Flutter placeholder) from the brand SVG mark, rasterized to PNG via sharp
# since flutter_launcher_icons' documented config expects PNG sources, not
# SVG (pub.dev/packages/flutter_launcher_icons). Run with:
#   dart run flutter_launcher_icons
# adaptive_icon_background is the exact flat brand color documented as the
# canonical "Implementation Snippet" background in
# docs/KwayaProBrandIndentity.html (also the literal fill already used in
# kwayapro_app_icon.svg) — not an arbitrary/guessed fill. Android-only per
# Phase 6b's scope; iOS icons are untouched (see PHASE_6B_REPORT.md).
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon_source/icon_full_1024.png"
  adaptive_icon_background: "#FFD97D"
  adaptive_icon_foreground: "assets/icon_source/icon_foreground_1024.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Audio assets for studio and player
  assets:
    - assets/audio/piano/
    - assets/audio/metronome/
    - assets/icons/
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
