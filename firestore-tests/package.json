{
  "name": "kwayapro-firestore-tests",
  "version": "1.0.0",
  "description": "Firestore + Storage security rules test suite for KwayaPro, testing against the real firestore.rules/storage.rules at the repo root via the Firebase emulator. Run with: firebase emulators:exec --project=kwayapro-rules-test \"mocha rules.test.js --timeout 60000\"",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.test.json --project=kwayapro-rules-test \"mocha rules.test.js --timeout 60000\""
  },
  "license": "UNLICENSED",
  "type": "commonjs",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "mocha": "^11.7.6"
  }
}
